%div(ui-view)
  %div( ng-if='projects' )
    %h1 Projects:
    %ul
      %li( ng-repeat='project in projects' )
        {{ project.title }}
        %a( ui-sref='projects.show({id: {{project.id}} })' ) details
