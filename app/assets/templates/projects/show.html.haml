%div( ui-view )
  %div( ng-if='project' )
    %h2 {{ project.title }}
    %table.table.table-responsive
      %thead
        %td Description
        %td Owner #
        %td Tasks
        %td Actions
      %tr
        %td( ng-if='project.description')
          {{ project.description }}
        %td( ng-if='!project.description')
          No description given
        %td {{ project.ownerId }}
        %td
          %a( ui-sref='project.tasks' )
            .btn.btn-primary.btn-sm Edit Tasks
        %td
          %a( ui-sref='project.edit' )
            .btn.btn-success.btn-sm Edit
          .btn.btn-danger.btn-sm( ng-click='delete()' ) Destroy
    %div
      %h4 Members of a project:
      %p( ng-hide='members' ) There is no members except you
      %div( ng-if='members' )
        %ul( ng-repeat='member in members')
          %li
            {{ member.email }} -
            %a( ng-click='removeMember(member.id)' ) kick
      .input-group
        %select.form-control( ng-model="selected" ng-options="user.email for user in usersForInvite" )
        .input-group-addon.btn-like( ng-click='sendInvite(selected.id)' ) invite